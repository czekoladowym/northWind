import { Sidebar } from "../components/Sidebar";
import { Header } from "../components/Header";

export const Orders = () => {
  return (
    <div>
      <Sidebar />
      <Header />
    </div>
  );
};
